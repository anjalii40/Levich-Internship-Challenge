# `src/lib`

Shared infrastructure for the app (no business logic).

Suggested contents over time:

- `socket/`: socket client setup
- `http/`: fetch helpers, interceptors
- `storage/`: localStorage/sessionStorage helpers
- `logger/`: logging utilities

